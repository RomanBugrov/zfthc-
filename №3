#include<iostream>
using namespace std;
 
int main() {
 int a, b, count;
 cin >> a >> b;
 count = 0;
    for (int i = a; i <= b; ++i) {
        if (i % 2) {
 ++ count;
        }
    }
 cout << count;
}
